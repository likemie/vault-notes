# Vault Use

本文件规定 AI 如何**检索、追踪、综合并输出**本知识库中的既有资料。它主要服务于 PPT 演示底稿／演讲稿、标准论文／书籍写作和主题报告，不替代 `vault-schema.md` 中关于新建、修改和同步正式知识条目的规则。

`output/` 是知识库的**操作台与成品工作区**，用于保存围绕具体任务形成的研究问题、证据地图、论证地图、写作提纲、草稿和最终输出。`output/` 中的文件不是正式知识条目，不进入 `wiki/` 的知识对象体系，也不参与 `related_*`、citation 或 source record 的自动关系维护。

---

## 1. 基本原则

- 把本库当作一张有出处的知识网络，而不是一组互不相干的 Markdown 文件。
- `wiki/index.json` 只用于定位入口，不能代替阅读条目正文。
- `related_*` 只表示**当前条目正文向外链接**过相应知识对象。它们由 `scripts/wiki_relations.py` 自动生成，不等于人工判断的同义、支持、反对或因果关系，也不是完整的双向关系图。
- 正文是关系的事实来源；`related_*` 在同步前可能暂时滞后。需要完整证据面时，应补做精确的反向链接检索。
- 沿 `related_*` 找到相邻条目后，必须阅读正文中链接出现的段落，判断它与当前问题的实际关系；不得仅凭 YAML 列表推断关系。
- 非 Argument 条目不直接维护 `sources`。它们的证据路径通常是 `related_arguments`，再由 Argument 的 `sources` 回到原始文献。
- 先利用已提炼的 wiki 条目和 Argument 建立问题结构；只有在需要核对原话、页码、数字、图表、方法细节或现有摘要无法回答时，才进入 `sources/` 或 `books/` 阅读原文件。
- 只检索当前问题需要的分支。不要为了“全面”而无目标地扫描整个 vault，也不要把高连接度条目中的所有链接都读一遍。
- 回答只能使用实际读到的材料。未打开原文件时，不得声称已经“核对原文”。

---

## 2. 知识对象及其作用

| 类型 | 回答的问题 | 在探索中的作用 |
|---|---|---|
| Concept | 术语是什么、边界在哪里 | 议题入口、近义概念和争议焦点 |
| Theory | 为什么会发生、如何解释 | 解释机制和分析视角 |
| Argument | 某篇论文或章节如何论证 | 研究问题、证据、推理、发现和局限 |
| Fact | 在何时何地发生了什么 | 政策、组织、项目、事件和制度情境 |
| Method | 结论如何产生 | 研究设计、资料收集和分析方法 |
| Instrument | 构念如何被测量 | 量表、测验、问卷、量规和观察工具 |
| Person | 谁提出、传播或实施 | 思想来源、作者关系和行动者 |
| Source record | Argument 来自哪份文件 | 原文件入口及反向 `extracted_to` 记录 |

`summary` 用于快速筛选，正文用于理解；两者都不能替代需要核验时的原始 PDF 或 EPUB。

---

## 3. `output/` 操作台

复杂学术任务默认使用 `output/` 作为临时研究与成品工作区。知识库负责提供可追溯的知识和证据，`output/` 负责承载围绕当前任务形成的组织、综合和写作结果。

### 3.1 基本定位

`output/` 与 `wiki/`、`sources/`、`books/` 的职责严格区分：

```text
wiki/       已提炼并可复用的知识对象与 Argument
sources/    已处理论文、报告及其原始文件入口
books/      书籍、章节及其原始文件入口
output/     当前任务的分析、综合、草稿与最终成品
```

`output/` 中允许出现尚在发展的分析框架、综合判断和写作结构，但这些内容不得反向当作知识库证据。需要引用事实、文献结论或数据时，仍必须回溯到 `wiki/` Argument 或原始 source。

### 3.2 任务文件夹

对于需要系统综合的 PPT／演讲稿、论文／书籍章节和主题报告，在 `output/` 下为当前任务建立独立文件夹。文件夹使用简洁、稳定、可识别的英文名称，不把临时日期或无意义编号作为唯一名称。

推荐结构：

```text
output/
  <project-name>/
    00-brief.md
    01-evidence-map.md
    02-argument-map.md
    03-outline.md
    draft.md
    final.md
```

这些文件按任务需要创建，不要求机械齐全：

- `00-brief.md`：题目、任务类型、中心问题、子问题、范围边界、用户要求；
- `01-evidence-map.md`：问题维度、初步判断、支持证据、反例、机制、Argument、Source 和核验状态；
- `02-argument-map.md`：核心判断、子判断、证据链、竞争性解释、条件与限制；
- `03-outline.md`：由证据和 Argument Map 反向形成的最终结构；
- `draft.md`：正在形成的完整底稿、论文／章节或报告草稿；
- `final.md`：当前任务已经完成并可交付的最终文本。

简单任务可以只创建一个工作文件；复杂任务才使用完整结构。不要为了形式完整而生成没有实际内容的空文件。

### 3.3 工作规则

1. **先研究，后成文。** 对复杂任务，先完成问题化和知识探索，再逐步形成 Evidence Map、Argument Map 和 outline；不要一开始就在 `final.md` 中边搜边写。
2. **工作文件可以迭代。** 新证据出现后，可以修改初步判断、Argument Map 和 outline；不得为了保持旧结构而压制相反证据。
3. **证据始终回指知识库。** Evidence Map 和草稿中的关键材料应保留 Argument、APA 短引用、source record 或页码等定位信息，不能只复制一段失去出处的文字。
4. **`output/` 不产生新的证据。** AI 在 `output/` 中形成的综合判断、分类、框架和建议属于当前任务的分析结果，不能在后续任务中被当作已发表研究或原始资料引用。
5. **成品与知识条目分离。** 用户要求修改正式 Concept、Theory、Fact、Method、Instrument、Person 或 Argument 时，仍按 `vault-schema.md` 执行；不得把 `output/` 草稿直接移动到 `wiki/` 代替正式入库流程。
6. **避免操作台污染知识网络。** `output/` 不纳入普通 wiki 自动补链和关系同步；其中可以使用 vault wikilink 帮助定位材料，但这些链接只服务于工作过程。
7. **任务结束保留可复查路径。** 最终成品应能够通过 Evidence Map 或正文引用追溯到实际使用的 Argument 和 Source，使以后修改 PPT、论文或报告时能够重新核验。


---

## 4. 标准探索流程

### 第一步：把主题转化为研究问题与分析维度

当用户给出的是题目、主题或写作任务，而不是已经明确的研究问题时，不要立即按关键词搜索。先把主题问题化，形成一个用于指导知识探索的临时分析框架。

至少明确：

1. **中心问题** 最终需要解释、比较、判断或论证什么；
2. **子问题** 中心问题可以拆成哪些相互关联但功能不同的问题；
3. **分析维度** 哪些维度可能决定结论，例如概念边界、理论机制、历史阶段、制度情境、研究方法、测量方式、作用条件和争议；
4. **范围边界** 时间、地区、教育阶段、对象、文献类型或用户指定的其他限制；
5. **证据要求** 任务需要定义、解释、事实、实证结果、竞争性观点、方法细节，还是原文核验。

初始分析框架只用于指导检索，**不等于最终目录或最终论证结构**。知识探索完成后，应根据实际证据重新组织结构，不得机械沿用“定义—历史—现状—问题—建议”等预设模板。

### 第二步：把问题维度转化为检索词

针对每个核心问题维度分别提取：

1. 核心概念及其中英文变体、缩写；
2. 可能涉及的理论、人物、国家、政策或组织；
3. 时间、地区、教育阶段和研究方法等限制条件；
4. 能够识别直接 Argument、Fact、Method 或 Instrument 的专门术语。

后续探索以问题维度为单位建立局部证据网络，避免从单个高连接度条目无差别向外扩张。

### 第三步：用索引确定入口

先读 `wiki/index.json`，以 `title`、`aliases`、`type` 和 `path` 查找候选条目。优先打开与问题直接匹配的 1–3 个入口，不要从目录名猜测文件路径，也不要把索引结果本身当作答案。

如果没有精确匹配：

1. 依次尝试中文名、英文名、缩写和常见变体；
2. 在 `wiki/index.json` 中做包含匹配；
3. 再对 `wiki/` 正文做有范围的 `rg` 检索；
4. 仍无结果时，明确说明本库没有直接条目，并使用最接近的条目或 Argument 继续查找。

推荐定位命令：

```bash
jq --arg q '检索词' '.[] | select(
  (.title | ascii_downcase | contains($q | ascii_downcase)) or
  ((.aliases // []) | map(ascii_downcase) | any(contains($q | ascii_downcase)))
)' wiki/index.json
```

中文或混合检索也可直接使用：

```bash
rg -n -i '检索词' wiki/index.json
```

### 第四步：完整阅读入口条目

至少读取：

- frontmatter 中的 `summary`、`type`、`related_*`；
- 正文的定义、边界、核心命题或论证结构；
- 与当前问题直接相关的段落及其中的 wikilink；
- 正文中的引用位置与页码。

先写出一个临时问题框架，例如“定义—机制—情境—证据—争议—局限”。后续只沿能补充这些维度的链接前进。

### 第五步：沿 `related_*` 有选择地扩展

把候选链接放入待读队列，并记录已经访问的条目，避免循环。每次扩展都必须回答：“这个条目能补足当前问题的哪个部分？”

一般优先级如下：

1. **`related_arguments`** 寻找直接证据、不同作者立场和研究局限；
2. **`related_theories` / `related_concepts`** 补充解释机制、定义边界和竞争性解释；
3. **`related_facts`** 补充政策、地区、组织和历史情境；
4. **`related_methods` / `related_instruments`** 判断证据如何产生、能支持多强的结论；
5. **`related_persons`** 追踪思想来源或行动者网络；
6. **`related_arguments` 中被多个已读条目共同指向的 Argument** 优先视为可能的枢纽文献，但仍需阅读正文验证。

这不是固定排序。问题类型决定路径：

| 问题类型 | 推荐路径 |
|---|---|
| 概念或议题综述 | Concept → Argument → Theory / 相邻 Concept → Source |
| 理论解释 | Theory → 核心 Concept → 应用该理论的 Argument → Source |
| 学术争论 | 多个 Argument → 各自 Concept / Theory → 各自 Source |
| 政策、国家或组织 | Fact → 相关 Argument → Concept / Theory → Source |
| 方法或测量 | Method / Instrument → 使用它的 Argument → 构念 → Source |
| 学者或思想脉络 | Person → Theory / Concept → Argument → Source |

对每个核心问题维度默认先探索两跳。只有第二跳仍出现与问题直接相关的新机制、新立场、关键证据、反例或重要限制时才继续第三跳。不要按连接数量决定相关性；`related_count` 高只说明链接较多。

### 第六步：补查反向链接

`related_*` 不是反向链接索引。入口条目可能没有列出所有引用它的 Argument，因此在完成第一轮扩展后，用入口条目的**规范 title** 做一次精确反查，重点检查 `wiki/arguments/`：

```bash
rg -l -F '[[规范 Title' wiki/arguments
```

例如：

```bash
rg -l -F '[[Learning Analytics' wiki/arguments
```

只把结果当作候选文件，随后仍要打开命中段落判断语境。若标题可能重复，使用 `wiki/index.json` 给出的完整路径和条目类型消歧；不要用过短的中文 alias 做全库反查。必要时再对 Theory、Fact、Method 或 Person 做同样的精确反查。

### 第七步：回到链接语境验证关系

对于准备采用的每一条关系，在来源条目正文中找到相应 wikilink，记录它属于哪一种关系，例如：

- 定义或组成；
- 理论解释；
- 支持性证据；
- 反例、批评或限制；
- 方法或测量；
- 历史、地区或组织背景；
- 仅顺带提及。

“仅顺带提及”的链接不能承担核心论证。若两个条目的 YAML 相互列出，但正文没有足够语境，也不能自行补出关系含义。

### 第八步：判断是否已经充分

对每个核心问题维度进行一次**证据饱和检查**。根据问题性质检查以下适用方面，不要求与问题无关的维度机械齐全：

- 核心定义与概念边界是否清楚；
- 主要机制或理论解释是否得到说明；
- 关键事实与适用情境是否明确；
- 是否至少有一条直接 Argument 证据；核心判断应尽量有两个彼此独立的来源支持；
- 若库中存在不同立场、相反结果或竞争性解释，是否已经读取并比较；
- 是否知道关键证据所用的方法、样本或测量方式，从而判断结论强度；
- 是否识别反例、自述局限、适用条件和尚未解决的问题；
- 是否能够追溯到 source record；核心结论、精确数字和关键引文必要时是否已经回到原文件。

新增 Argument 若不再改变当前判断、机制解释、适用边界、反例或证据强度，该问题维度可以停止扩展。若某一重要维度在库中缺失，应明确记录为证据缺口，不要用常识填补。

---

## 5. 从知识探索到学术综合

完成第一轮探索后，不应立即按照读取顺序写作。知识库中的 Concept、Theory、Fact、Method、Instrument 和单篇 Argument 都是形成最终论证的材料；最终成品必须围绕当前任务重新组织。

### 5.1 建立证据地图

先按核心问题维度整理已读材料。推荐内部记录：

```text
问题维度 | 初步判断 | 支持证据 | 反例/限制 | 机制/条件 | Argument | 方法/情境 | Source/页码 | 核验层级
```

其中“初步判断”不是预设结论，而是当前证据能够支持到什么程度的暂时判断。随着新材料加入，应允许修改、收缩或放弃原判断。

证据地图至少承担四个作用：

1. 把分散条目重新归入用户真正要回答的问题；
2. 区分直接证据、解释机制、背景事实、反例和限制；
3. 暴露只有观点而缺少证据、只有单篇研究而缺少独立支持、或缺少适用条件的部分；
4. 防止同一篇 Argument 被反复用于多个强结论而造成证据虚增。

### 5.2 判断材料之间的综合关系

不要把多篇文献机械写成“研究 A 认为……研究 B 认为……”。阅读多个 Argument 后，判断它们在当前问题中属于哪一种关系：

- **累积关系** 不同研究以独立证据支持相近判断；
- **机制关系** 不同材料分别解释条件、过程、中介机制或结果，能够组成较完整的解释链；
- **竞争关系** 理论、结果或解释相互冲突，需要比较证据基础；
- **条件关系** 表面冲突可能来自对象、情境、时间、实施方式、方法或测量不同，应说明结论在什么条件下成立；
- **历史演化关系** 材料反映同一制度、概念或实践在不同时期的变化，应识别阶段、转折及其机制；
- **层级关系** 材料分别位于个体、课堂、学校、区域、国家或全球等层次，需要说明层次之间如何连接。

同一组材料可以同时具有多种关系，但必须依据已读正文和证据判断，不得为了形成漂亮结构而自行制造联系。

### 5.3 从 Evidence Map 形成 Argument Map

**单篇 Argument 条目不是最终成品中的一个论点。** Argument 条目记录某篇论文、章节、报告或专著如何论证；最终写作中的论点应是综合多个知识对象和文献证据后形成的可讨论判断。

正式写作前，为每个核心判断建立简要 Argument Map：

```text
核心判断
├─ 子判断 / 前提
│  ├─ 直接证据
│  ├─ 理论或机制解释
│  └─ 适用条件
├─ 独立支持证据
├─ 反例 / 竞争性解释
└─ 综合后的边界与结论强度
```

核心判断必须能够回答“这一部分究竟要说明什么”。如果某一部分只能概括为一个主题名，而不能形成可讨论的判断，说明材料尚未完成综合。

### 5.4 重构最终论证框架

初始分析框架服务于检索；最终论证框架由证据反向形成。完成 Evidence Map 和 Argument Map 后，应重新判断：

1. 哪些初始子问题已经得到充分回答；
2. 哪些原先认为重要的维度实际证据不足，应弱化或删除；
3. 是否出现了新的机制、分化、历史转折或竞争性解释，应提升为核心部分；
4. 各部分之间应形成何种逻辑关系：递进、因果、比较、条件、历史演化、层级解释、反驳或限制；
5. 最终中心判断是否比初始题目更加具体，并且能够被现有证据支持。

不得把 Concept、Theory、Fact、Argument 等知识库类型直接转换成成品目录，也不得按照文献读取顺序组织正文。

### 5.5 核验层级

内部可使用以下核验层级，防止多跳探索中混淆“找到”“读过”和“核验过”：

- **L0 — Index hit** 只在索引或检索结果中发现，尚未阅读正文；只能作为候选线索。
- **L1 — Entry read** 已阅读知识条目相关正文，可用于理解本库如何组织该知识对象，但不能据此声称已核对原作者。
- **L2 — Argument checked** 已阅读对应 Argument 的相关论证、证据和页码，可以准确复述本库对该文献的提炼。
- **L3 — Source verified** 已打开原始 PDF、EPUB 或其他来源并核对相关上下文，可说明原文、页码、数字、图表、方法或作者实际表述已经核验。

最终回答不必展示这些等级，但任何“已核对原文”的表述必须达到 L3。

### 5.6 三类主要输出模式

知识探索、Evidence Map 和 Argument Map 是三类任务的共同基础。只有在最终组织阶段，才根据成品类型调整结构和表达。

#### A. PPT 演示底稿 / 演讲稿

PPT 和演讲稿应形成清楚的**认知推进路线**，而不是知识点目录。通常需要依次完成：提出问题 → 建立必要背景 → 暴露需要解释的现象或矛盾 → 给出证据 → 解释机制或差异 → 形成核心判断 → 说明含义。

具体规则：

- 一页或一个主要段落原则上承担一个明确 message，而不是只写一个宽泛 topic；
- 页面标题优先写成能够表达判断的标题，而不是“背景”“理论”“现状”等中性标签；
- 文献、数据、案例和概念都服务于当前 message，不按作者逐篇介绍；
- 复杂议题应设计前后页之间的递进关系，使听众能够理解“为什么下一步要讨论这个问题”；
- 演讲稿可以比 PPT 页面保留更多证据、限定条件和过渡，但不得为了口语效果夸大证据；
- 若用户要求制作 PPT 内容，先形成完整的 narrative structure，再决定分页，不要先按固定页数切割材料。

#### B. 标准论文 / 书籍写作

论文和书籍写作采用最高的证据与论证要求。除用户另有要求外：

- 每一节围绕一个核心判断展开，而不是围绕若干文献展开；
- 每个核心判断都要检查直接证据、独立支持、竞争性结果、方法差异、情境条件和重要限制；
- 事实、数字、作者主张、实证结果和机制解释均应能够追溯；
- 若一个核心结论主要依赖单篇文献，应优先回到原文件核验；
- 对相互冲突的研究，不能简单计数多数与少数，应比较研究对象、方法、测量、时间和情境；
- 文献综述的功能是建立问题、概念边界、已有解释和证据缺口，不是逐篇摘要；
- 最终结论的强度不得超过证据能够支持的范围。

#### C. 主题报告

主题报告强调围绕现实问题形成有证据支持的结构化判断。根据任务性质，可以采用“问题界定—现状与变化—关键发现—机制解释—分化与风险—综合判断—可能应对”等结构，但不得机械套用。

具体规则：

- Fact、政策、组织和项目等现实材料通常承担描述现状和变化的主要证据；
- Argument 用于解释事实、比较模式和判断已有研究支持到什么程度；
- Theory 用于解释机制，不应为了理论完整而压过现实问题；
- 比较型报告应先确定共同比较问题和维度，再识别不同模式及差异形成机制；
- 政策建议必须能够从前文发现和机制分析中推出；知识库证据不足时，应明确区分“证据支持的判断”和“基于现有材料提出的推论或建议”。


---

## 6. 从条目回到原始文件

### 6.1 普通论文和报告

典型路径：

```text
Concept / Theory / Fact / Method / Instrument / Person
  → related_arguments
  → Argument 正文与「## 来源」
  → Argument frontmatter 的 sources
  → sources/<记录名>.md
     或 sources/<记录名>/<记录名>.md
  → 同目录 PDF、正文嵌入文件或 iframe 指向的原文件
```

执行规则：

1. 非 Argument 条目先沿 `related_arguments` 找到承载证据的 Argument。
2. 在 Argument 中同时查看正文 `## 来源` 和 frontmatter `sources`。`sources` 是由正文来源段落生成的定位字段。
3. 解析完整 wikilink 目标。例如 `[[sources/Li_2026_CEAI/Li_2026_CEAI|Li_2026_CEAI]]` 对应 vault 根目录下的 `sources/Li_2026_CEAI/Li_2026_CEAI.md`。
4. source record 中的 `![[文件名.pdf]]` 或 `![[文件名.epub]]` 相对于该 source record 所在目录；优先打开同目录本地文件。
5. `iframe` URL 是远程阅读入口，不代表本地文件一定存在。先检查 source record 同目录，再决定是否使用远程地址。
6. source record 的 `extracted_to` 是反向索引，说明该原文件已提炼到哪些 Argument；它有助于从原文件反查论证，但不代替 Argument 正文。

### 6.2 书籍、论文集和章节

典型路径：

```text
知识条目
  → related_arguments
  → wiki/arguments/books/<书籍目录>/Argument_*.md
  → sources 中的 books/<书籍目录>/<来源记录>
  → books/<书籍目录>/<来源记录>.md
  → 同目录章节 PDF、整书 PDF 或 EPUB
```

执行规则：

- 章节 Argument 优先使用它自己的章节 source record 和章节 PDF，而不是直接用整书文件。
- `part_of` 指向书籍 overview 或整书记录。需要理解全书结构、章节位置或编者总体主张时，再沿 `part_of` 进入 overview。
- 若只有整书 PDF / EPUB，没有独立章节文件，使用 Argument 的章节范围、目录或页码在整书文件中定位。
- 论文集 overview 用于了解全书结构；具体主张应尽量回到对应章节 Argument 和章节原文。
- 不要因为 Argument 文件名含书名，就假定 `books/` 中原文件同名；以 `sources` wikilink 的完整路径为准。

### 6.3 何时必须读原文件

出现以下任一情况时，应打开 PDF、EPUB 或其他原文件，而不是只依赖 Argument：

- 用户要求原文、逐字引文、页码或图表；
- 需要核对精确数字、样本、量表、模型设定或统计结果；
- 不同条目对同一结论的表述有冲突；
- Argument 的摘要不足以支撑将要给出的强结论；
- 需要判断作者究竟是在主张、转述、假设还是批评某个观点；
- 需要查看上下文以避免断章取义。

引用或复述时区分证据层级：

- **已读原文** 可以说明已由原文件及页码核对；
- **仅读 Argument** 说明依据本库的 Argument 提炼，并保留其中页码；
- **仅由相邻条目提及** 只能作为线索，不能包装成已经核验的文献结论。

`raw/` 是待处理材料，不是常规证据入口。只有用户明确要求处理未入库材料，或 `sources/`、`books/` 中确实缺少所需原文件时，才在限定范围内查找 `raw/`。

---

## 7. 综合与回答要求

最终回答应围绕用户的问题和已经形成的 Argument Map 组织，而不是按“读过哪些文件”罗列笔记。至少做到：

1. 先给出中心判断、核心问题或清楚的议题结构；
2. 以“综合主张及其根据”为基本单位组织内容，不以单篇文献为基本单位；
3. 区分事实、作者主张、理论解释和 AI 基于多项材料形成的综合判断；
4. 对支持证据、竞争性结果、限制和适用条件分别交代；不能把“存在多篇相关文献”自动写成“已经形成共识”；
5. 每个关键结论都用 APA 作者—年份格式指向已读的 Argument 或原文件；
6. 标明尚未核对原文的内容、证据不足的部分和库内资料缺口；
7. 用户要求来源时，除 APA 引用外，按需给出可定位的 vault 文件路径、Argument 名称和原文页码；
8. 不把 `related_*` 本身当作证据，不把多个条目的共同出现误写成因果关系或学术共识；
9. 如果现有证据不能支持初始题目隐含的判断，应修改判断，而不是寻找材料迎合预设结论。

### APA 引用规则

知识库中的内容都应能沿知识条目、Argument 和 source record 追溯来源。来源没有直接写在当前条目的 YAML 中，不等于没有来源；AI 必须继续沿正文引用或 `related_arguments` 查到 Argument。写作时必须把这条追溯关系转换成 APA 引用，不能只给 wikilink、文件名或笼统写“知识库显示”。

1. **短引用以 Argument YAML 为准。** 优先读取 Argument frontmatter 的 `citation_aliases`、`authors` 和 `year`，使用其中已经确定的作者—年份形式，例如 `(Ball, 2008)`、`(Li et al., 2026)`。不要自行翻译作者姓名或重新判断 `et al.`。
2. **一般使用 APA 后引。** 先以连贯的学术语言直接陈述主张、证据及论证过程，再把作者—年份引用放在相关句子或段落之后。通常写“某种制度安排会改变行动者获得资源的方式（Author, Year）”，不写“Author（Year）说／认为／指出某种制度安排会改变行动者获得资源的方式”。只有在比较不同作者的立场、追踪概念归属、讨论学术争论或作者身份本身具有分析意义时，才使用叙述式引用。
3. **精确主张带页码。** 若知识条目或 Argument 正文已经提供页码，应写成 `(Author, Year, p. 12)` 或 `(Author, Year, pp. 12–15)`。Argument 内部针对当前文献只写的 `(p. 12)`，在对外写作时必须结合该 Argument YAML 恢复为完整作者—年份后引。
4. **完整参考文献以 `citation` 为准。** 文末参考文献表直接采用 Argument YAML 的完整 `citation` 字段，不凭记忆补写作者、刊名、卷期、出版社或 DOI。
5. **同作者同年以后缀为准。** 出现 `2008a`、`2008b` 等情形时，使用 Argument 的 `citation_aliases` 或 `citation/citation_full.json` 已确定的后缀，不自行分配。
6. **一个综合判断可有多个来源。** 多篇文献共同支持时，在同一括号中列出并以分号分隔，例如 `(Author, 2020; Author & Author, 2022)`；页码分别附在各自文献之后。
7. **不得伪造引用。** 如果相关 Argument 缺少有效 `citation`、`citation_aliases` 或年份，应继续查 source record 和原文件；仍不能确认时，明确标注资料缺口，不猜测 APA 信息。
8. **引用知识条目时回溯其 Argument。** Concept、Theory、Method、Instrument、Fact 和 Person 页不是最终文献出处。使用其中的定义、命题、事实或方法说明时，应依据相关正文中的 Argument 引用或 `related_arguments` 找到原始 Argument，再引用该 Argument YAML 对应的 APA 文献。
9. **区分引用与库内定位。** APA 引用承担学术来源标注；vault 文件路径和 wikilink 用于帮助用户定位本地材料。需要时二者可以同时提供，但路径不能替代 APA 引用。

除非用户只要求简短查找结果，较完整的写作应在正文使用 APA 短引用，并在末尾列出“参考文献”，逐条采用已实际使用的 Argument YAML `citation`。参考文献表只列正文真正引用过的材料。

### 学术写作风格

输出采用严谨、质朴、克制的学术语态。写作的基本单位是“核心主张及其根据”；修辞姿态和材料堆积本身不构成论证。

1. **围绕核心主张组织。** 每一节应有清楚、可讨论的中心判断。每个重要段落先提出一个具体主张，再提供相关概念界定、文献证据、事实材料或推理过程，并在必要时说明适用条件与限制。
2. **建立主张与证据之间的联系。** 不要在引文或数字之后直接跳到结论。应说明该材料支持主张的哪一部分、其推理如何成立，以及证据能够支持多强的判断。
3. **减少无信息量的否定转折。** 少用“不是……而是……”“并非……而是……”“绝非……而是……”等句型。只有在正文已经存在两个明确、相互竞争且需要辨析的命题时才使用。不得为了制造强调而先虚构一个无人主张的反面观点。
4. **删除元话语和空泛过渡。** “值得注意的是”“不难发现”“显而易见”“由此可见”“需要指出的是”“从某种意义上说”等表达，如果没有增加判断、证据或限定，应直接删除或改写为实质性内容。
5. **避免口号式二分。** 不使用“从 X 走向 Y”“从工具到伙伴”“既是……更是……”“表面上……实质上……”等现成框架替代具体分析。确有历史变化或层次差异时，应说明变化发生的时间、条件、机制和证据。
6. **保持概念稳定。** 同一概念在全文中使用一致译名；首次出现时按需给出英文名称或缩写，后文不反复堆叠中英文。不要为了语言变化而随意替换近义词，造成概念漂移。
7. **避免无根据的评价。** 不使用“重要”“关键”“深刻”“卓越”“先进”“领先”“创新”等评价词代替论证。若要判断某项研究或政策的重要性，应说明其影响范围、相对于何种基准的差异，以及支持判断的材料。
8. **控制修辞和情绪强度。** 避免夸大、浮夸、煽情、戏剧化或悬念化的网文辞藻；不用感叹号制造语气，不以反问句代替论证，不把复杂争议写成善恶、先进落后或成败对决。
9. **严禁互联网与商业宣推套话。** 不使用“赋能”“抓手”“闭环”“跃迁”“赛道”“痛点”“风口”“红利”“生态位”“组合拳”“方法论沉淀”“降维打击”“破局”“对标”“引爆”“全链路”“颗粒度”“底层逻辑”“顶层设计”等被过度滥用的黑话。若原始材料本身使用这类词，应加以界定、转述为准确概念，或明确标明为原文用语。
10. **避免 AI 模板化表达。** 不写“这不仅是……更是……”“在这一宏大背景下”“随着时代的滚滚浪潮”“为我们提供了全新的视角”“具有深远的理论与现实意义”等无法单独验证的套句。结尾应概括已经论证的结果、边界或待解决问题，不作拔高式收束。
11. **让段落承担论证功能。** 相邻段落之间应形成界定、递进、比较、因果、证据补充、反驳或限制关系。材料若不能支撑当前核心主张，应删去或移到真正相关的位置。

成文后逐段检查：

- 本段的核心主张能否用一句话准确概括？
- 本段是否提供了可追溯的证据或必要的推理？
- 引用材料与结论之间的关系是否已经说明？
- 是否存在删除后不影响信息的转折、强调、形容词或套话？
- 结论的强度是否超过证据能够支持的范围？
- 是否交代了重要的条件、例外、反例或局限？

一个简洁的内部记录格式可以是：

```text
问题维度 | 初步判断 | 条目 | 关系语境 | 支持/反例 | Argument | APA 短引用 | Source | 页码 | 核验层级
```

此记录用于防止在多跳探索中丢失出处，不必机械地展示给用户。

---

## 8. 只读探索时的禁止事项

- 不手工修改 `related_*`、Argument 的 `sources` 或 source record 的 `extracted_to`。
- 不为方便回答而运行会改写 vault 的同步脚本。
- 不根据文件名或 YAML 邻接表杜撰条目之间的关系。
- 不把 `wiki/index.json`、索引页、source record 或书籍 overview 当作原始学术证据。
- 不越过 Argument 直接把非 Argument 条目的概括归于原作者，除非已经核对相应原文。
- 不因追踪到第二、第三跳就默认材料更重要；始终以用户问题和正文语境为准。

若任务同时要求修改知识库，检索阶段遵守本文件，写入和同步阶段遵守 `vault-schema.md`、对应模板与专项 schema。
